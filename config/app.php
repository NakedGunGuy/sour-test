<?php

return [
    "name" => "Sauerkraut",
    "debug" => true,
    "timezone" => "UTC",

    // Default theme directory (relative to project root).
    // Per-app configs (frontend.php, cms.php) can override this.
    "theme" => "theme",

    // Default asset mode: 'inline' or 'link'.
    // Per-app configs can override this.
    "assets" => "link",
];
